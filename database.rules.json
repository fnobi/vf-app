{
  "rules": {
    ".read": false,
    ".write": false,
    "sandbox": {
      ".read": true,
      ".write": true
    },
    "userProfile": {
      "$user_id": {
        ".read": "$user_id === auth.uid",
        ".write": "$user_id === auth.uid"
      }
    }
  }
}
